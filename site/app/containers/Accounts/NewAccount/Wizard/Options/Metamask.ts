import messages from './Metamask.messages';
import { Option } from './Types';

export const Metamask: Option = {
  name: 'Metamask',
  stored: 'metamask',
  password: 'print',
  maxValue: 10000,
  convenience: 2,
  messages: messages,
};
