{
  "name": "@thecointech/tx-processor",
  "version": "0.2.13",
  "private": "true",
  "description": "The Coin: A command-line app for completing transactions",
  "keywords": [
    "THECoin"
  ],
  "author": "Stephen Taylor <stephen@thecoin.io>",
  "homepage": "http://thecoin.io",
  "license": "MIT",
  "scripts": {
    "build": "yarn tsc -b tsconfig.build.json",
    "clean": "rimraf ./build && rimraf ./coverage",
    "dev": "cross-env TS_NODE_PROJECT=../../tsconfig.dev.json node -r ts-node/register -r ../../__mocks__/mock_node ./src/main.ts",
    "start": "yarn build && node --inspect ./build/main.js",
    "test": "jest --coverage",
    "test:summary": "jest --coverage > ./coverage/summary.txt"
  },
  "files": [
    "build/**/*"
  ],
  "main": "./build/index.js",
  "exports": {
    ".": "./build/index.js",
    "./": "./build/"
  },
  "dependencies": {
    "@thecointech/logging": "^0.2.13",
    "@thecointech/rbcapi": "^0.2.13",
    "@thecointech/tx-processing": "^0.2.13"
  },
  "devDependencies": {
    "jest": "*"
  }
}
