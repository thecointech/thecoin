{
  "name": "rates-patcher",
  "version": "1.0.0",
  "main": "./build/index.js",
  "license": "MIT",
  "dependencies": {
    "@google-cloud/datastore": "^5.1.0",
    "@the-coin/utilities": "*"
  },
  "scripts": {
    "dev": "cross-env NODE_ENV=development && yarn build && yarn start",
    "start": "node -r dotenv/config ./build/index.js",
    "build": "yarn tsc -b tsconfig.build.json",
    "clean": "rimraf ./build"
  }
}
