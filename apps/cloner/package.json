{
  "name": "@thecointech/cloner",
  "version": "0.2.21",
  "private": true,
  "dependencies": {
    "@thecointech/contract-core": "^0.2.21"
  },
  "scripts": {
    "fetch": "cross-env CONFIG_NAME=prod cross-env TS_NODE_PROJECT=../../tsconfig.dev.json node -r ts-node/register ./src/fetch.ts",
    "dev": "cross-env TS_NODE_PROJECT=../../tsconfig.dev.json node -r ts-node/register ./src/main.ts",
    "dev:live": "cross-env CONFIG_NAME=devlive yarn dev",
    "prod:test": "cross-env CONFIG_NAME=prodtest yarn dev"
  }
}
