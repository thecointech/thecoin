{
  "name": "@the-coin/broker-service",
  "version": "0.1.1",
  "private": true,
  "description": "TheCoin simple broker services.  This sample API is an example of how to setup the simplest low-volume exchange, when combined with the included server code, without external dependencies",
  "keywords": [
    "TheCoin",
    "ethereum"
  ],
  "author": "Stephen Taylor <stephen@thecoin.io>",
  "homepage": "http://thecoin.io",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/thecointech/thecoin.git",
    "directory": "apps/rates-service"
  },
  "scripts": {
    "start": "node ./build/main.js",
    "dev": "cross-env NODE_ENV=development && yarn build && node --inspect ./build/main.js",
    "build": "yarn build:api && yarn build:src",
    "build:src": "yarn tsc -b -v tsconfig.build.json",
    "build:api": "yarn build:routes && yarn build:swagger",
    "build:routes": "yarn tsoa routes",
    "build:swagger": "yarn tsoa swagger",
    "build:live": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/index.ts",
    "test": "yarn jest --runInBand",
    "test:coverage": "yarn test --coverage"
  },
  "engines": {
    "node": "14.x.x"
  },
  "files": [
    "build/**/*"
  ],
  "main": "./build/main.js",
  "dependencies": {
    "@the-coin/utilities": "latest",
    "cors": "^2.8.5",
    "express": "^4.16.4",
    "google-auth-library": "^5.6.1",
    "googleapis": "47.0.0",
    "swagger-express-middleware": "3.0.0-alpha.5",
    "swagger-tools": "^0.10.4",
    "swagger-ui-express": "^4.0.2",
    "tsoa": "^2.5.13"
  },
  "devDependencies": {
    "@types/swagger-express-middleware": "^1.0.10",
    "@types/swagger-ui-express": "^4.1.1",
    "jest": "*"
  }
}
