import React from 'react';
import { Meta } from '@storybook/react';
import { Editor } from '.';

export default {
  title: 'NFT/Editor',
  component: Editor,
} as Meta;

export const Footer = () => {
  return <Editor />
}
