{
  "name": "the-coin-broker-sample",
  "version": "0.1.0",
  "description": "TheCoin simple broker services.  This sample API is an example of how to setup the simplest low-volume exchange, when combined with the included server code, without external dependencies",
  "main": "index.js",
  "scripts": {
    "start": "node build/index.js",
    "debug": "cross-env NODE_ENV=development && npm run build && node --inspect index.js",
    "build": "tsc -p . && npm run build:copy",
    "build:copy": "(if not exist build\\api mkdir build\\api) && copy Generated\\api src\\api && copy src\\api build\\api",
    "build:live": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/index.ts"
  },
  "keywords": [
    "swagger"
  ],
  "license": "Unlicense",
  "private": true,
  "dependencies": {
    "@google-cloud/firestore": "^2.4.0",
    "@the-coin/utilities": "^0.1.64",
    "@types/node-mailjet": "^3.3.0",
    "cors": "^2.8.5",
    "ethers": "^4.0.29",
    "express": "^4.16.3",
    "googleapis": "^39.2.0",
    "grpc": "^1.20.3",
    "jest": "^24.8.0",
    "js-yaml": "^3.3.0",
    "node-mailjet": "^3.3.1",
    "swagger-tools": "0.10.4",
    "typescript": "^3.5.2"
  },
  "devDependencies": {
    "@types/jest": "^24.0.13",
    "@types/node": "^12.0.2",
    "ts-jest": "^24.0.2",
    "ts-node": "^8.1.1"
  }
}
