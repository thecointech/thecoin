import status from './status.json';

// This file purely exists to allow our test files to import status
// without VS squiggly lines (as our tsconfig doesn't apply to test files)
export default status;