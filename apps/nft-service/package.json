{
  "name": "@thecointech/nft-service",
  "version": "0.2.1",
  "private": true,
  "description": "TheCoin NFT service.  Permits gassless transactions with the TheCoin NFT contract",
  "keywords": [
    "TheCoin",
    "ethereum"
  ],
  "author": "Stephen Taylor <stephen@thecoin.io>",
  "homepage": "http://thecoin.io",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/thecointech/thecoin.git",
    "directory": "apps/rates-service"
  },
  "scripts": {
    "start": "node ./build/main.js",
    "dev": "yarn run-env dotenv_config_path=../../environments/development.env",
    "dev:live": "yarn run-env dotenv_config_path=../../environments/devlive.env",
    "prod:test": "yarn run-env dotenv_config_path=../../environments/prodtest.env",
    "run-env": "node --inspect=9231 -r ts-node/register -r dotenv/config ./src/main.ts",
    "predeploy": "ts-node --skip-project predeploy",
    "deploy": "run-s deploy:src deploy:cron",
    "deploy:src": "gcloud app deploy",
    "deploy:cron": "gcloud app deploy cron.yaml",
    "build": "run-s build:api build:src",
    "build:src": "yarn tsc -b tsconfig.build.json",
    "build:api": "yarn tsoa spec-and-routes",
    "clean": "run-p clean:*",
    "clean:build": "rimraf ./build",
    "clean:test": "rimraf ./coverage",
    "test:summary": "jest --coverage > ./coverage/summary.txt",
    "test": "jest --coverage"
  },
  "engines": {
    "node": "14.x.x"
  },
  "files": [
    "build/**/*"
  ],
  "main": "./build/main.js",
  "dependencies": {
    "@pinata/sdk": "1.1.20",
    "@thecointech/logging": "^0.2.1",
    "@thecointech/utilities": "^0.2.1",
    "@tsoa/runtime": "^3.8.0",
    "axios": "*",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "js-yaml": "^3.13.1",
    "luxon": "1.26.0",
    "node-fetch": "^2.6.0",
    "swagger-tools": "^0.10.4",
    "swagger-ui-express": "^4.1.6",
    "multer": "^1.4.2",
    "image-type": "^4.1.0"
  },
  "devDependencies": {
    "@types/multer": "^1.4.5",
    "@types/node-fetch": "^2.5.5",
    "@types/swagger-ui-express": "^4.1.1",
    "jest": "*",
    "tsoa": "^3.8.0"
  }
}
