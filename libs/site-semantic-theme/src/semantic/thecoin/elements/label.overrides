/*******************************
         Site Overrides
*******************************/

// No built-in variable for line-height in labels
.ui.label {
  line-height: @lineHeight;
  float: left;
}

// Labels inside form fields (like your Form story)
.ui.form .field > .ui.label {
  padding: 0.625rem 0;
  background: transparent;
}
