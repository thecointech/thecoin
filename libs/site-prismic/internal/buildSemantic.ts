import "@thecointech/site-semantic-theme/build";

// site-semantic doesn't support less, so we generate the
// CSS custom properties from the Less variables to ensure consistency
import "@thecointech/site-semantic-theme/buildCssVars";
