import constants from './constants.css';

export const TransitionDuration = parseInt(constants.TransitionDuration);
export const SidebarWidth = parseInt(constants.SidebarWidth);

// // We can't type this, you just have to look it up in source!
// const LessVars = require('!!../../../../tools/less-var-loader!./semantic/globals/site.variables');
// console.log('Loaded: ' + JSON.stringify(LessVars));
// export { LessVars }
