import React from "react";

export const StoreMicrosoft = (props: { children: React.ReactNode }) => {

    return (
      <>
          <a>
            {props.children}
          </a>
      </>
    );
  }
