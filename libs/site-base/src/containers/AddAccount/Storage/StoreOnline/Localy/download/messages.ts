/*
	Offline Backup Storage Option
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Download';

export default defineMessages({
  buttonText: {
    id: `${scope}.buttonText`,
    defaultMessage: 'DOWNLOAD',
	}
});