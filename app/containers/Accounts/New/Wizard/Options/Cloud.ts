import messages from './Cloud.messages'
import { Option } from './Types';

export const Cloud: Option = {
	name: 'Online Storage',
	stored: 'cloud',
	password: 'print',
	maxValue: 10000,
	messages: messages
}