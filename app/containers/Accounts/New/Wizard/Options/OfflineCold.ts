import messages from './OfflineCold.messages'
import { Option } from './Types';

export const OfflineCold: Option = {
	name: 'Offline Cold Storage',
	stored: 'safetyBox',
	maxValue: 0,
	messages: messages
}