name: Build and Test

on:
  pull_request:
    branches: [ dev ]
  push:
    branches: [ dev ]

jobs:
  build:
    uses: ./.github/jobs/build.yml

  test:
    needs: build
    uses: ./.github/jobs/test.yml