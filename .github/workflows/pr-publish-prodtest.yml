name: Publish to Prodtest

on:
  pull_request:
    branches: [ publish/prodtest ]
    types: [ closed ]

jobs:
  publish:
    uses: ./.github/workflows/pr-publish.yml
    with:
      environment: prodtest
    secrets: inherit
